// Compatibility entry point: never manufacture athletes to meet a count.
// Add reviewed real identities to player-seeds.json and player-identities.json.
// This command deliberately only validates; it does not mutate the catalog.
await import("./verify-catalog-sizes.mjs");
